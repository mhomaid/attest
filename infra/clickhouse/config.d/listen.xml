<clickhouse>
    <!-- Bind on all interfaces so Docker can forward host and inter-container traffic. -->
    <listen_host>0.0.0.0</listen_host>
    <listen_host>::</listen_host>
    <listen_try>1</listen_try>
</clickhouse>
