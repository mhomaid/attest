[variables]
NODE_VERSION = "20"

[phases.install]
cmds = ["bun install --frozen-lockfile"]

[phases.build]
cmds = ["cd apps/workbench && bun run build"]

[start]
cmd = "cd apps/workbench && bun run start"
