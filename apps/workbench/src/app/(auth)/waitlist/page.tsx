import { WaitlistForm } from "./waitlist-form";

export const metadata = {
  title: "Request access — Attest",
  description: "Join the Attest waitlist. The workbench is invite-only.",
};

export default function WaitlistPage() {
  return <WaitlistForm />;
}
